<h1>Browse all activities</h1>
<div class="all_feed_list">
  <% @activities.each do |activity| %>
    <div class="activity">
      <%= link_to activity.owner.user_name, activity.owner if activity.owner %>
      <%= render_activity activity %>
    </div>
  <% end %>
</div>
