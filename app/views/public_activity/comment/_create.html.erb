added a comment

<% if activity.trackable %>
  <%= link_to activity.trackable.body, activity.trackable.recipe %>
  <%= time_ago_in_words(activity.created_at) %>
<% else %>
  which has since been removed
<% end %>
