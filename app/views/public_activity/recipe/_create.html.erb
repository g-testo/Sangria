added a recipe called

<% if activity.trackable %>
  <%= link_to (image_tag activity.trackable.recipe_image, height: "30"), activity.trackable %>
  <%= link_to activity.trackable.name, activity.trackable %>
  <span class="feed_time"><%= time_ago_in_words(activity.created_at) %> ago </spam>
<% else %>
  This Recipe has been removed
<% end %>
